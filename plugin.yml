name: "tag-release"
description: Tag commits with release tag when deploying
author: james.bebbington@rokt.com
public: true
requirements:
  - bash
  - git
configuration:
  properties:
    release_complete:
      type: boolean # Should we mark the commit as current or pending
  additionalProperties: false